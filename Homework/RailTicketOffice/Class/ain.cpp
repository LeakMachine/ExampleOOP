/*#include "class.h"

int main() {

	RailTicketOffice rto;

	Date datenig;
	FullName fn;

	fn.name = "A";
	fn.surname = "B";
	fn.patronymic = "C";

	datenig.day = 1;
	datenig.month = 1;
	datenig.year = 2020;

	rto.orderAccept(datenig, 69, 1, fn, 0, "Govno", "Mocha");

	rto.print2(0);

	int c = rto.orderCountPrice(0);

	std::cout << c << std::endl;

	if (rto.orderCheckAvailability(0) == true) {
		std::cout << "good" << std::endl;
	}
	else {
		std::cout << "bad" << std::endl;
	}

	rto.print();

	return 0;
*/